# Catologo-de-peliculas
# Catologo-de-peliculas
🚀 Cómo correr el proyecto
Opción 1: Abrir directamente el HTML

Descarga o clona el repositorio.

Abre el archivo index.html con doble clic en tu navegador.

Opción 2: Usar Live Server (recomendado)

Abre el proyecto en VS Code.

Instala la extensión Live Server.

Haz clic derecho en index.html.

Selecciona "Open with Live Server".


⚖️ 3 diferencias que noté entre Flexbox y Grid

Control de filas y columnas

Con Flexbox el control principal es en una dimensión (fila o columna).

Con Grid puedes controlar filas y columnas al mismo tiempo (2 dimensiones).

Distribución del espacio

En Flexbox, si no se controla bien el flex-grow, las cards pueden crecer cuando hay pocos elementos.

En Grid, las columnas se mantienen más estables usando grid-template-columns.

Layouts complejos (mosaico)

Con Grid es mucho más fácil crear diseños tipo mosaico usando grid-column y grid-row.

Con Flexbox, lograr ese efecto es más limitado y menos natural.
